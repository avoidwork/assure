/**
 * Label pack
 *
 * @private
 * @type {Object}
 */
var label = {
	invalidArguments : "Invalid arguments",
	promiseRejected  : "Rejected: {{outcome}}",
	promiseResolved  : "Resolved: {{outcome}}"
};
